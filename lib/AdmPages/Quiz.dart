import 'package:flutter/material.dart';

class QuizTelaADM extends StatelessWidget{
  const QuizTelaADM({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.white70,
      ),
    );
  }
}